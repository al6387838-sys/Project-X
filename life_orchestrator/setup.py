from setuptools import setup, find_packages

setup(
    name="life_orchestrator",
    version="1.0.0",
    description="PROJECT-X Life Orchestrator — SPRINT 013",
    packages=find_packages(),
    python_requires=">=3.9",
)
