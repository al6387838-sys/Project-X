from setuptools import setup, find_packages

setup(
    name="evolution_engine",
    version="1.0.0",
    description="PROJECT-X Evolution Engine — SPRINT 011",
    packages=find_packages(),
    python_requires=">=3.9",
)
