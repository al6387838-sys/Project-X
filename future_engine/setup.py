from setuptools import setup, find_packages

setup(
    name="future_engine",
    version="1.0.0",
    description="PROJECT-X Future Engine — SPRINT 012",
    packages=find_packages(),
    python_requires=">=3.9",
)
